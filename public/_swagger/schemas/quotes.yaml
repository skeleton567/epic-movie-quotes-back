components:
  user:
    type: object
    properties:
      id:
        $ref: "../schemas/auth.yaml#/components/id"
      name:
        $ref: "../schemas/auth.yaml#/components/name"
      email:
        $ref: "../schemas/auth.yaml#/components/email"
      image:
        $ref: "../schemas/movies.yaml#/components/image"
  
  quote_en:
    type: string
    patern: Only english letters and spaces
    default: You shall not pass

  quote_ka:
    type: string
    patern: Only georgian letters and space
    default: შენ ვერ გახვალ

    
