  parameters:
    id:
      name: id
      in: path
      description: The Id of the resource
      required: true
      schema:
        type: integer
        minimum: 1
        default: 1
        
    hash:
      name: hash
      in: path
      description: The hash code generated by server
      required: true
      schema:
        type: string
    
    movie_id:
      name: movie
      in: path
      description: Id of movie
      required: true
      schema:
        type: string
        minimum: 1
        default: 1
    
    page:
      name: page
      in: query
      description: Page number
      required: true
      schema:
        type: integer
        minimum: 1
        default: 1

    quote_id:
      name: quote
      in: path
      description: Id of quote
      required: true
      schema:
        type: string
        minimum: 1
        default: 1

    email:
      name: email
      in: path
      description: Email adress
      required: true
      schema:
        type: string
      

    
      
     